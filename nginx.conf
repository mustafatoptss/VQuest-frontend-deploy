server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # React/Vite router yapısının düzgün çalışması için gerekli:
        try_files $uri $uri/ /index.html;
    }
}
